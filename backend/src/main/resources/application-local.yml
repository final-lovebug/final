app:
  # http://localhost로 로컬 실행할 때 Secure 쿠키는 브라우저가 돌려보내지 않으므로 끈다.
  auth:
    cookie:
      secure: false
  ai:
    dispatch:
      # AI 워커는 로컬에 없다. 큐 대신 인프로세스 대역이 빈 결과를 돌려준다(D-74).
      # 실제 워커를 붙여 보려면 dev 프로파일(LocalStack)로 띄운다.
      mode: in-process
