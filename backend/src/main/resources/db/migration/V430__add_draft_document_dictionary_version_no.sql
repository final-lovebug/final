-- 대조에 쓴 사전집 버전을 초안에 고정한다(D-93).
-- 사전집 초안이 진행 중이어도 문서를 갱신할 수 있게 되면서, 발행 시점의 활성 버전을 찍으면
-- 대조하지 않은 버전을 기준으로 표시하게 된다. 이 컬럼이 그 기준을 초안에 얼려 둔다.
-- 이 마이그레이션 이전에 만들어진 초안은 값이 없어 null이며, 발행 시 종전대로 활성 버전을 쓴다.
alter table draft_document
    add column dictionary_version_no int null;
